{
  "name": "rpa-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agente local Playwright para automação opt-in do feed Shopee Video. NÃO-OFICIAL. Ver AUDIT.md.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "setup-login": "tsx src/setup-login.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured'",
    "test": "echo 'no tests yet' && exit 0",
    "playwright:install": "playwright install chromium"
  },
  "dependencies": {
    "@publica/shared": "workspace:*",
    "pino": "^9.5.0",
    "pino-pretty": "^11.3.0",
    "playwright": "^1.48.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
